{{ form_start(paintForm) }}
<div class="te-form-error">
    {{ form_errors(paintForm) }}
</div>

    {{ form_widget(paintForm) }}


<button type="submit"  class="btn te-btn" >Valider</button>
{{ form_end(paintForm) }}
