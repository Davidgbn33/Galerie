<nav class="tp-bg navbar navbar-expand bg-body-tertiary ">

    <div class="te-container te-grid-tel">
        <a class="navbar-brand te-grid-title" href="/">
            <h1 class="te-font-title">Toiles et Papiers</h1>
        </a>
        <div class="collapse navbar-collapse te-grid-content" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <div class="te-tel-position">
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item m-auto">
                            <a class="te-a te-font-dropdown text-decoration-none me-3"
                               href="{{ path('admin_list') }}">ADMIN</a>
                        </li>
                    {% endif %}
                    <li class="nav-item dropdown te-a">
                        <a class="nav-link dropdown-toggle te-font-dropdown te-a ps-0" href="#" role="button"
                           data-bs-toggle="dropdown" aria-expanded="false">
                            CATEGORIES
                        </a>
                        <ul class="dropdown-menu">
                            {% set counter = 1 %}
                            {% for category in categories %}
                                <li>
                                    <a class="dropdown-item" href="{{ path('show_cat',{ slug: category.slug}) }}">
                                        <img src="{{ asset('uploads/images/paint/') }}{{ category.categoryImage }}"
                                             alt="" class="te-img-cat">
                                        <span class="te-label-cat{{ counter }}">{{ category.categoryName }}</span>
                                    </a>
                                </li>
                                {% set counter = counter + 1 %}
                            {% endfor %}
                        </ul>
                    </li>
                </div>
            </ul>


            {% if app.user %}

                <a href="/login" class="d-flex flex-column text-center te-color me-3 text-decoration-none   ">
                    <i class="fa-regular fa-user te-navbar-icon" style="color: #f5f5dd;"></i>
                    <p class="text-center m-0 te-color-log">{{ app.user }}</p>
                </a>

                <a href="/logout" class="d-flex flex-column text-center te-color me-3 text-decoration-none  ">
                    <i class="fa-solid fa-right-from-bracket te-navbar-icon" style="color: #f5f5dd;"></i>
                    <p class="text-center m-0 te-color-log">Déconnexion</p>
                </a>
            {% else %}
                <a href="/login" class="d-flex flex-column text-center te-color me-3 text-decoration-none   ">
                    <i class="fa-regular fa-user te-navbar-icon" style="color: #f5f5dd;"></i>
                    <p class="text-center m-0 te-color-log">Connexion</p>
                </a>

            {% endif %}
        </div>
    </div>
</nav>
